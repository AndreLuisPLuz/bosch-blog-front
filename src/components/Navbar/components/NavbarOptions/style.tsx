import styled from "styled-components";

const Container = styled.div`
    height: 100%;
    width: fit-content;

    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1rem;

    padding: 0 1rem 0 1rem;
`;

export { Container };