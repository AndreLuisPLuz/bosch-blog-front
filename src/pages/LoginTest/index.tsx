import { ReactNode } from "react";
import LoginModal from "../../modules/authentication/pages/LoginModal";

const LoginTest = (): ReactNode => {
    return (
        <LoginModal>

        </LoginModal>
    );
};

export default LoginTest;